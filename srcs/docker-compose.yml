version: '3.8'

services:
  portainer:
    image: portainer/portainer-ce:latest
    container_name: portainer
    restart: on-failure
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    ports:
      - "9001:9000"
    networks:
      - inception
  adminer:
    build:
      context: ./requirements/adminer
      dockerfile: Dockerfile
    container_name: adminer
    image: adminer
    ports:
      - 8080:8080
    restart: on-failure
    init: true
    networks:
      - inception
  vsftpd:
    build:
      context: ./requirements/vsftpd
      dockerfile: Dockerfile
    container_name: vsftpd
    image: vsftpd
    volumes:
      - files:/var/www/html
    ports:
      - '20-21:20-21'
      - '30000-30009:30000-30009'
    env_file:
      - .env
    restart: on-failure
    init: true
    networks:
      - inception
  redis:
    build:
      context: ./requirements/redis
      dockerfile: Dockerfile
    container_name: redis
    image: redis
    ports:
      - '6379:6379'
    volumes:
      - files:/data
    restart: on-failure
    networks:
      - inception
  mariadb:
    build:
      context: ./requirements/mariadb
      dockerfile: Dockerfile
    container_name: mariadb
    image: mariadb
    ports:
      - '3306:3306'
    volumes:
      - db:/var/lib/mysql
    env_file:
      - .env
    restart: on-failure
    networks:
      - inception
  wordpress:
    build:
      context: ./requirements/wordpress
      dockerfile: Dockerfile
    container_name: wordpress
    image: wordpress
    ports:
      - '9000:9000'
    volumes:
      - files:/var/www/html
    env_file:
      - .env
    restart: on-failure
    depends_on:
      - mariadb
      - redis
    networks:
      - inception
  nginx:
    build:
      context: ./requirements/nginx
      dockerfile: Dockerfile
    container_name: nginx
    image: nginx
    ports:
      - '443:443'
    volumes:
      - files:/var/www/html
    restart: on-failure
    depends_on:
      - wordpress
    networks:
      - inception

networks:
  inception:
    name: inception

volumes:
  db:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/axbrisse/data/db
  files:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/axbrisse/data/files
  portainer_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/axbrisse/data/portainer_data